# Review 3 — demo script and readiness checklist (panel: 16 Sep 2026)

Deliverables (all in the repo root, regenerated by the scripts under `scripts/review3/`):

| file | what | rebuild |
|---|---|---|
| `Review3_Presentation.pptx` / `.pdf` | 18-slide panel deck, rubric-mapped (slide 2) | `python scripts/review3/build_deck.py` then export via PowerPoint |
| `Review3_Report.docx` / `.pdf` | Chapters 1–3 (+ §3.8 revisions), Chapter 4 initial, Chapter 5 outline, references | `python scripts/review3/build_report.py` then Word: update TOC, export |
| `assets/r3_*.png` | figures for both, drawn from `artifacts/reports/*.json` | `python scripts/review3/make_figures.py` |
| `artifacts/reports/RESULTS.md` | every table the deck quotes | `make results` |

The three build scripts run on the **Windows-side** Python (matplotlib, python-pptx, python-docx
are there); everything else runs in WSL as usual.

## Before the review — things only the team can do

1. **Slide 3 / §3.8:** paste the panel's Review 2 comments from the evaluation sheet and the
   action taken on each (rubric parameter 1 — 1 mark is *only* for this).
2. **Slide 16 / §4.7:** fill the individual-contribution table honestly. The rubric grades
   *demonstrated* contribution per member; the deck currently shows the Review-2 plan with «…»
   placeholders. `git log`/PROGRESS.md are the evidence.
3. **Slide 15 / §4.7:** confirm the AI-tool acknowledgement wording with the guide (guideline §2
   requires significant AI use to be acknowledged).
4. **Slide 17:** confirm a publication venue with the guide — Review 4 awards 2 marks only against
   *proof of submission*, and its date is 12–16 Oct.
5. Guide must see the deck before the panel (as for Review 2).
6. Laptop **on mains power** for the demo (GPU downclocks on battery) and the WSL venv warm:
   run `bash scripts/run.sh test -q` once in the morning.

## Pre-flight (morning of the review)

1. Laptop on **mains power**; close other GPU users.
2. `wsl -d Ubuntu -e bash -lc "pgrep -fa harness"` → nothing running; `nvidia-smi` → < 1 GB used.
3. Start the server **with the ASR pre-warmed** (loads TF-IDF, translator and Whisper at startup, ~20 s):
   ```bash
   wsl -d Ubuntu -e bash -lc "cd /mnt/c/Users/madha/Downloads/fir-tn-capstone && FIR_PREWARM=1 bash scripts/run.sh serve"
   ```
   then `http://127.0.0.1:8000/v1/health` must show `"asr_loaded": true` and `"error": null`.
4. In Edge, open `http://127.0.0.1:8000/`, click **● Record** once and allow the microphone so the
   permission prompt does not appear in front of the panel.
5. Copy one FLEURS clip to the desktop for the upload fallback and for the guard demo:
   `data/fleurs/ta_in/audio/test/12583250098003224463.wav` (clip 1916, the replay-loop case) and
   `.../16989398024822917306.wav` (clip 1721).
6. `bash scripts/run.sh test -q` once (all green) — proves the tree is the one on the slides.

## Demo (≈6 minutes) — run in this order

Open `http://127.0.0.1:8000/`. Demo-mode URLs pre-fill and run a sample on load:

| step | URL / action | what to point at |
|---|---|---|
| 1 | `/?sample=theft&run=1` | route **OFFICER_REVIEW**: classifier abstains on a 2-sentence complaint (finding 5); *Also consider* shows IPC 379 → BNS 303(2) **cognizable** (value ₹15,000 resolves the ₹5,000 split) and 380; BNS text shown; "Mark verified" disabled |
| 2 | edit the text: delete "worth Rs 15,000" → Draft | 379 now **conditional** — "cognizability depends on property value"; nothing defaults |
| 3 | `/?sample=dowry death&run=1` | route **FIR** via BNS 80; BNS 85 shows *conditional* (who reports); element checks ✓/? with quoted words; BNS gazette text above the IPC lineage text |
| 4 | `/?sample=spoken Tamil&run=1` | Tamil input → ITN turns "ஐம்பதாயிரம் ரூபாய்" into ₹50,000 and fills the slot; *Classifier read* row shows the opus-mt translation |
| 5 | **● Record** → speak the complaint in Tamil (e.g. the "spoken Tamil" sample aloud, ~15 s) → **■ Stop** | the page posts the browser recording to `/v1/complaint/audio`; the transcript appears in the box, ITN turns "ஐம்பதாயிரம் ரூபாய்" into ₹50,000, the same decision renders — *speech-driven end to end, all local* |
| 6 | Upload the FLEURS clip 1916 → **Transcribe & draft** | the transcript guard fires (`repetition_loop`) and the page says the draft cannot be auto-resolved — a fabrication caught, not hidden |
| 7 | Look up a section: type `85` → Look up | gazette heading + text, Schedule entry *conditional* with the Schedule's words, bailable/court |
| 8 | (if time) `python -m fir draft --json "..."` in WSL | same decision as JSON — the API and CLI share one graph |

Say the honest number: WER 51% / CER 15% zero-shot on read Wikipedia sentences (before this week: 57% / 22%,
four times slower), two of sixty clips flagged for a repetition loop — see the sweep result on slide 13.
Every draft is appended to `artifacts/audit/drafts.jsonl` (record id, route, sections, hash of the text —
never the text or the audio); `/v1/health` shows `audit_entries`.

## Fallbacks

- Microphone refused or no sound card → upload the FLEURS clip (step 6) or the text samples.
- GPU busy or server fails to load → CLI: `bash scripts/run.sh` is not needed; from WSL:
  `cd src && HF_HUB_OFFLINE=1 python -m fir draft "On 8.5.2026 an unknown man stole my mobile phone worth Rs 15,000 from my shop."`
- Nothing runs → the PDF deck has the same outputs on slides 10–12, and `artifacts/reports/RESULTS.md`
  has every number.

## Questions the panel is likely to ask — and the one-line answers

- *Why does the classifier find nothing on your own theft sample?* Domain shift (finding 5): it is
  trained on ILSI court fact-statements; its top-5 for that complaint does not contain theft at
  all, so a lower threshold would apply wrong sections. In-domain synthetic data is the fix; the
  cue scan carries short inputs meanwhile — and it is shown as a question, never applied.
- *Why TF-IDF and not the legal BERT you proposed?* Measured on the full corpus: 0.700 vs 0.526
  micro-F1 with every lever applied; ensemble +0.0006. The encoder's job is element verification.
- *Is 51% WER usable?* Not as a transcript to file — as decision support with provenance, with
  CER 15% (Tamil word boundaries inflate WER), with fabrication guards, and with IndicWhisper /
  a code-switch fine-tune as the planned next steps. The number is honest and zero-shot.
- *Who signs off the legal tables?* Nobody yet: parsed from the gazette with page citations,
  spot-checked by 25 tests, awaiting a legal read-through — the form says so.
- *What is "conditional"?* The Schedule's own wording: abetment follows the offence, s.85 depends on
  who reports, theft splits at ₹5,000. It routes to the officer and quotes the condition.
- *Can the system file an FIR?* No path exists: no endpoint, CLI or node sets `verified`; tests
  fail if one appears.
