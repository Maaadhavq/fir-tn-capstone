# ---------------------------------------------------------------------------
# FIR-TN core requirements (Python 3.11, WSL2 Ubuntu)
#
# NOTE: `torch` is intentionally NOT listed here. It must be installed from the
# CUDA 12.8 index (see setup_wsl.sh) because this machine's GPU is a Blackwell
# RTX 5050 (sm_120), which older cu121 wheels do not support.
#
# NOTE: HuggingFace `datasets` is deliberately EXCLUDED. It depends on pyarrow,
# which Smart App Control blocks on the Windows host. All our loaders read plain
# JSONL / TSV / tar, and DuckDB covers any parquet we hit later.
# ---------------------------------------------------------------------------

# --- core ML / NLP ---
transformers==4.46.3
tokenizers==0.20.3
safetensors==0.4.5
accelerate==1.1.1
scikit-learn==1.5.2
scipy==1.14.1
numpy==2.1.3
joblib==1.4.2

# --- ASR ---
faster-whisper==1.1.0
ctranslate2==4.8.2         # CUDA 12.8 / Blackwell build; 4.5.0 had no sm_120 kernels
av==13.1.0
soundfile==0.12.1

# --- translation (Marian / NLLB tokenizers) ---
sentencepiece==0.2.0
sacremoses==0.1.1
sacrebleu==2.4.3
pdfplumber==0.11.10        # scripts/build_bnss_schedule.py (gazette PDF -> First Schedule CSV)

# --- metrics ---
jiwer==3.0.5
seqeval==1.2.2

# --- data ---
duckdb==1.1.3
huggingface_hub==0.26.3
pyyaml==6.0.2
tqdm==4.67.1

# --- orchestration / schema ---
langgraph==0.2.53
langchain-core==0.3.21
pydantic==2.10.3

# --- serving ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
httpx==0.28.1            # TestClient

# --- dev / test ---
pytest==8.3.4
ruff==0.16.7
